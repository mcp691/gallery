<p id="notice"><%= notice %></p>
<% if signed_in? && current_user.admin? %>
<%= link_to 'New Product', new_product_path, class: "btn btn-primary", style: "color:white; margin: 2%;" %>
<% end %>
<% provide(:title, "Artwork") %>

<div class="prod-screen" align=" center">
  <% @products.each do |product| %>
  <%= render product %>
  <% end %>
</div>
<br>
